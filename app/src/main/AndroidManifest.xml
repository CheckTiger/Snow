<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.sxh.snowfox">

    <!-- 网络权限的请求 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="design_width"
            android:value="720" />
        <meta-data
            android:name="design_height"
            android:value="1280" />

        <activity
            android:name=".UI.activity.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.activity.NewsActivity" />
        <activity android:name=".UI.activity.ClockViewActivity" />
        <activity android:name=".UI.activity.MiStepViewActivity" />
        <activity android:name=".UI.activity.BankActivity" />
        <activity android:name=".UI.activity.TaoBaoSaleProgressViewActivity" />
        <activity android:name=".UI.activity.ViewStudyActivity" />
        <activity android:name=".UI.activity.PaintActivity"></activity>
        <service android:name=".service.LocalIntentService"/>
    </application>

</manifest>